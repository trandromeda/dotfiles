{
  // Enable all markdownlint rules
  "default": true,

  // Disable line length check for tables and code blocks
  "MD013": { "line_length": 120, "code_blocks": false, "tables": false },

  // Disable annoying repeated header error
  "MD024": false,

  // Disable line length check for tables and code blocks
  "MD026": { "punctuation": ".,;:" },

	// Allow some HTML
	"MD033": { "allowed_elements": ["kbd"] },

  // Fenced code blocks do not need language spec
  "MD040": false,

  // Exclude code block style
  "MD046": false
}
